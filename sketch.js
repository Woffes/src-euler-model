let dy
let c = 299792
let rs = 25
let r = 1000 //y værdi
let t = 0 //x værdi
let yn

function setup() {
  dy=-c*(1-rs/r)*sqrt(rs/r)
  createCanvas(700, 700)
  return dy
}

function draw() {
  background(220);
  antalPunkter()
}

function antalPunkter(){
  let punkter=[]
  let i
  for(let i = 0; 0 < i, i++;)
  punkter[i]=[]
}